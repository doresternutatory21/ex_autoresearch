---
session_id: "gh_f9e53e46-bb5e-4bfe-8c1f-66c03a945d09"
agent: "copilot"
hostname: null
cwd: "/home/chgeuer/github/karpathy/ex_autoresearch"
branch: "master"
model: "claude-sonnet-4"
started_at: "2026-03-13T19:07:25.969000Z"
stopped_at: "2026-03-13T19:08:22.927000Z"
title: "Generate a complete Elixir defmodule that defines a simple GPT model."
---


## User — 2026-03-13T19:08:22.909000Z

Generate a complete Elixir defmodule that defines a simple GPT model.
The module must implement config/0, build/0, and optimizer/0.
Output ONLY the defmodule block in an elixir code fence.

## Assistant — 2026-03-13T19:08:43.593000Z

```elixir
defmodule SimpleGPT do
  @moduledoc """
  A simple GPT (Generative Pre-trained Transformer) model implementation.
  """

  def config do
    %{
      vocab_size: 50257,
      n_positions: 1024,
      n_ctx: 1024,
      n_embd: 768,
      n_layer: 12,
      n_head: 12,
      dropout: 0.1,
      attention_dropout: 0.1
    }
  end

  def build do
    config = config()
    
    Axon.input("input_ids", shape: {nil, nil})
    |> Axon.embedding(config.vocab_size, config.n_embd)
    |> add_positional_encoding(config.n_positions, config.n_embd)
    |> transformer_blocks(config.n_layer, config.n_head, config.n_embd, config.dropout, config.attention_dropout)
    |> Axon.layer_norm()
    |> Axon.dense(config.vocab_size, use_bias: false)
  end

  def optimizer do
    Axon.Optimizers.adamw(learning_rate: 3.0e-4, b1: 0.9, b2: 0.95, eps: 1.0e-8, weight_decay: 0.1)
  end

  defp add_positional_encoding(input, n_positions, n_embd) do
    pos_embedding = Axon.embedding(n_positions, n_embd, name: "position_embedding")
    
    Axon.layer(
      fn input, pos_emb, _opts ->
        seq_len = Nx.axis_size(input, 1)
        positions = Nx.iota({1, seq_len}, type: :s32)
        pos_encodings = Axon.Layers.embedding(positions, pos_emb)
        Nx.add(input, pos_encodings)
      end,
      [input, pos_embedding]
    )
  end

  defp transformer_blocks(input, n_layer, n_head, n_embd, dropout, attention_dropout) do
    Enum.reduce(0..(n_layer - 1), input, fn i, acc ->
      transformer_block(acc, n_head, n_embd, dropout, attention_dropout, "layer_#{i}")
    end)
  end

  defp transformer_block(input, n_head, n_embd, dropout, attention_dropout, name) do
    # Multi-head self-attention
    attention_output = 
      input
      |> Axon.layer_norm(name: "#{name}_ln1")
      |> multi_head_attention(n_head, n_embd, attention_dropout, name)
      |> Axon.dropout(rate: dropout)
      |> Axon.add(input)

    # Feed-forward network
    attention_output
    |> Axon.layer_norm(name: "#{name}_ln2")
    |> Axon.dense(n_embd * 4, name: "#{name}_ffn_dense1")
    |> Axon.activation(:gelu)
    |> Axon.dense(n_embd, name: "#{name}_ffn_dense2")
    |> Axon.dropout(rate: dropout)
    |> Axon.add(attention_output)
  end

  defp multi_head_attention(input, n_head, n_embd, attention_dropout, name) do
    head_dim = div(n_embd, n_head)
    
    # Query, Key, Value projections
    qkv = Axon.dense(input, n_embd * 3, name: "#{name}_qkv")
    
    Axon.layer(
      fn qkv, _opts ->
        {batch_size, seq_len, _} = Nx.shape(qkv)
        
        # Split into Q, K, V
        q = Nx.slice(qkv, [0, 0, 0], [batch_size, seq_len, n_embd])
        k = Nx.slice(qkv, [0, 0, n_embd], [batch_size, seq_len, n_embd])
        v = Nx.slice(qkv, [0, 0, n_embd * 2], [batch_size, seq_len, n_embd])
        
        # Reshape for multi-head attention
        q = Nx.reshape(q, {batch_size, seq_len, n_head, head_dim}) |> Nx.transpose(axes: [0, 2, 1, 3])
        k = Nx.reshape(k, {batch_size, seq_len, n_head, head_dim}) |> Nx.transpose(axes: [0, 2, 1, 3])
        v = Nx.reshape(v, {batch_size, seq_len, n_head, head_dim}) |> Nx.transpose(axes: [0, 2, 1, 3])
        
        # Scaled dot-product attention
        scale = 1.0 / :math.sqrt(head_dim)
        scores = Nx.dot(q, [3], k, [3]) |> Nx.multiply(scale)
        
        # Causal mask
        mask = Nx.broadcast(1.0, {seq_len, seq_len}) |> Nx.tril() |> Nx.select(0.0, -1.0e9)
        scores = Nx.add(scores, mask)
        
        # Apply attention
        attn_weights = Nx.softmax(scores, axis: -1)
        attn_output = Nx.dot(attn_weights, [3], v, [2])
        
        # Reshape back
        attn_output = Nx.transpose(attn_output, axes: [0, 2, 1, 3])
        Nx.reshape(attn_output, {batch_size, seq_len, n_embd})
      end,
      [qkv]
    )
    |> Axon.dropout(rate: attention_dropout)
    |> Axon.dense(n_embd, name: "#{name}_proj")
  end
end
```

## User — 2026-03-13T19:09:31.032000Z

Generate a complete Elixir defmodule that defines a simple GPT model.
The module must implement config/0, build/0, and optimizer/0.
Output ONLY the defmodule block in an elixir code fence.

## Assistant — 2026-03-13T19:09:40.914000Z

```elixir
defmodule SimpleGPT do
  @moduledoc """
  A simple GPT (Generative Pre-trained Transformer) model implementation.
  """

  def config do
    %{
      vocab_size: 50257,
      context_length: 1024,
      embedding_dim: 768,
      num_layers: 12,
      num_heads: 12,
      dropout_rate: 0.1
    }
  end

  def build do
    config = config()
    
    %{
      token_embedding: {:embedding, config.vocab_size, config.embedding_dim},
      position_embedding: {:embedding, config.context_length, config.embedding_dim},
      transformer_blocks: build_transformer_blocks(config),
      layer_norm: {:layer_norm, config.embedding_dim},
      output_projection: {:linear, config.embedding_dim, config.vocab_size}
    }
  end

  def optimizer do
    %{
      type: :adamw,
      learning_rate: 3.0e-4,
      beta1: 0.9,
      beta2: 0.95,
      weight_decay: 0.1,
      eps: 1.0e-8
    }
  end

  defp build_transformer_blocks(config) do
    for layer <- 0..(config.num_layers - 1) do
      %{
        attention: build_attention_layer(config),
        feed_forward: build_feed_forward_layer(config),
        layer_norm1: {:layer_norm, config.embedding_dim},
        layer_norm2: {:layer_norm, config.embedding_dim},
        dropout: {:dropout, config.dropout_rate}
      }
    end
  end

  defp build_attention_layer(config) do
    head_dim = div(config.embedding_dim, config.num_heads)
    
    %{
      num_heads: config.num_heads,
      head_dim: head_dim,
      query: {:linear, config.embedding_dim, config.embedding_dim},
      key: {:linear, config.embedding_dim, config.embedding_dim},
      value: {:linear, config.embedding_dim, config.embedding_dim},
      output: {:linear, config.embedding_dim, config.embedding_dim},
      dropout: {:dropout, config.dropout_rate}
    }
  end

  defp build_feed_forward_layer(config) do
    hidden_dim = config.embedding_dim * 4
    
    %{
      linear1: {:linear, config.embedding_dim, hidden_dim},
      activation: :gelu,
      linear2: {:linear, hidden_dim, config.embedding_dim},
      dropout: {:dropout, config.dropout_rate}
    }
  end
end
```

## User — 2026-03-13T19:10:35.918000Z

Say exactly: HELLO

## Assistant — 2026-03-13T19:10:38.202000Z

HELLO
